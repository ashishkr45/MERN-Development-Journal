### What dose a component returns?
- It return a xml 
- JSX = JavaScript XML
- JSX allows us to write HTML-like code directly within JavaScript. This makes it easier to create
and manage the user interface in React applications.